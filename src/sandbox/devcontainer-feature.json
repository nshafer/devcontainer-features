{
  "id": "sandbox",
  "version": "2.0.0",
  "name": "Sandbox (nshafer)",
  "description": "Seals the host sockets VS Code forwards into a dev container - SSH agent, GPG agent, X11 and the Dev Containers extension IPC - and takes away the remote user's blanket sudo grant so the seals cannot simply be undone. Mitigation, not a boundary: the socket must exist for the container to attach, so there is a short window at each attach. See the README.",
  "documentationURL": "https://github.com/nshafer/devcontainer-features/tree/main/src/sandbox",
  "options": {
    "blockSshAgent": {
      "type": "boolean",
      "default": true,
      "description": "Block the forwarded SSH agent (/tmp/vscode-ssh-auth-*.sock). Your host SSH keys stop being usable from inside, so git over SSH stops working there."
    },
    "blockGpgAgent": {
      "type": "boolean",
      "default": true,
      "description": "Block the forwarded GPG agent (~/.gnupg/S.gpg-agent, .extra and S.keyboxd), by sealing the sockets just after VS Code creates them. Signed commits stop working inside the container."
    },
    "blockX11": {
      "type": "boolean",
      "default": true,
      "description": "Block the forwarded display (/tmp/.X11-unix/X*). The socket is sealed just after VS Code creates it, not pre-empted -- pre-empting it stops the container attaching at all. GUI apps stop reaching your desktop. Wayland cannot be blocked from inside - see the README."
    },
    "blockCodeCli": {
      "type": "boolean",
      "default": false,
      "description": "Block the 'code' CLI channel (vscode-ipc-*.sock). Off by default because sealing it stops the container attaching: the VS Code server unlinks that path when it disposes the hook, a root-owned tombstone makes that throw EPERM, and the window sits on 'Configuring Dev Container' for good. What it grants while open: driving the editor, and opening any URI with your host desktop's handler ('code --open-external'). Turn it on only if you accept the attach failure."
    },
    "blockGitAskpass": {
      "type": "boolean",
      "default": false,
      "description": "Block the git credential channel (vscode-git-*.sock, reached through GIT_ASKPASS). Off by default because blocking it stops git push, pull and fetch over HTTPS, in the VS Code UI and in the terminal. What it grants while open: the host's GitHub token, to anything in the container that connects to the socket. Turn it on if no host credential should be reachable, and authenticate some other way."
    },
    "blockExtensionIpc": {
      "type": "boolean",
      "default": true,
      "description": "Block the Dev Containers extension channel (vscode-remote-containers-ipc-*.sock). An HTTP POST on it calls rpc on the extension running on your host, and git uses it as credential.helper - so it answers for every host in your host's credential store, with no prompt. Blocking it costs the host credential helper and host docker registry logins. Push and pull still work through blockGitAskpass."
    },
    "scrubEnv": {
      "type": "boolean",
      "default": true,
      "description": "Also unset the variables that advertise the blocked sockets (SSH_AUTH_SOCK, DISPLAY, GIT_ASKPASS, BROWSER, VSCODE_IPC_HOOK_CLI, REMOTE_CONTAINERS_*) in every shell. Only for the channels that are actually blocked - unsetting the variable for an open channel would break it. Cosmetic next to the socket blocks, since VS Code re-injects them, but it stops tools finding the paths by accident."
    },
    "sweepInterval": {
      "type": "string",
      "default": "1",
      "proposals": [
        "1",
        "5"
      ],
      "description": "Seconds between backstop sweeps. Sealing is normally driven by inotify, within about a millisecond of a socket appearing; this poll only catches what inotify missed, so it rarely needs changing."
    },
    "sudoMode": {
      "type": "string",
      "default": "drop",
      "enum": [
        "drop",
        "restricted",
        "keep"
      ],
      "description": "What happens to the remote user's sudo grant. 'drop' removes it and everything that could restore it - the default, and the only mode where every seal in this feature holds. 'restricted' removes the blanket grant and replaces it with the linted allowlist in sudoCommands. 'keep' changes nothing, which makes this whole feature decoration. Named sudoMode and not sudo because a feature option becomes an image environment variable, and a container-wide SUDO=drop breaks the common '$SUDO apt-get install' idiom."
    },
    "sudoCommands": {
      "type": "string",
      "default": "",
      "description": "Commands the remote user may run as root when sudoMode is restricted, comma separated, each an absolute path with its arguments pinned: '/bin/systemctl restart myapp,/usr/sbin/nginx -s reload'. Every entry is linted at build time and the build fails on a route back to full root - a shell, an interpreter, a wildcard, an unpinned package manager, a firewall tool, a user-writable binary. This list becomes the trust boundary of the feature. See the README."
    },
    "sudoAllowUnsafe": {
      "type": "boolean",
      "default": false,
      "description": "Install the sudoCommands allowlist even when the lint rejects an entry. Off by default, and the build fails instead. Turn it on only after you have read each finding and decided it is wrong or acceptable - the findings are routes from an allowed command back to full root, not style notes."
    }
  },
  "entrypoint": "/usr/local/share/devcontainer/sandbox/entrypoint.sh",
  "postStartCommand": "/usr/local/share/devcontainer/sandbox/post-start.sh",
  "postAttachCommand": "/usr/local/share/devcontainer/sandbox/post-attach.sh",
  "containerEnv": {
    "BASH_ENV": "/usr/local/share/devcontainer/sandbox/scrub-env.sh"
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/common-utils"
  ]
}
