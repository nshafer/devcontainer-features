# Docker Hub needs three hosts, not one: registry, auth, and the CDN that serves the layers.
# Missing the CDN fails *after* authenticating, which is a confusing failure to debug.
registry-1.docker.io
auth.docker.io
production.cloudflare.docker.com
production.cloudfront.docker.com
# GitHub Container Registry.
ghcr.io
pkg-containers.githubusercontent.com
# Installing Docker, which is a different host from pulling with it. The docker-in-docker feature
# fetches the signing key and the packages from here at build time, and a block shows up as
# `curl: (22) ... error: 403` followed by `gpg: no valid OpenPGP data found`.
download.docker.com
