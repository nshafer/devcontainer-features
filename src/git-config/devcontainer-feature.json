{
  "id": "git-config",
  "version": "1.0.1",
  "name": "Git config (nshafer)",
  "description": "Copies the host's git configuration and excludes file into the container, from git's XDG config directory, in every container - including the ones the CLI creates, which copy nothing.",
  "documentationURL": "https://github.com/nshafer/devcontainer-features/tree/main/src/git-config",
  "options": {},
  "mounts": [
    {
      "source": "${localEnv:HOME}/.config/git",
      "target": "/mnt/git-config",
      "type": "bind,readonly"
    }
  ],
  "postCreateCommand": "/usr/local/share/nshafer-git-config/post-create.sh",
  "installsAfter": [
    "ghcr.io/devcontainers/features/common-utils"
  ]
}
