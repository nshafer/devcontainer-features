{
  "id": "git-config",
  "version": "2.0.0",
  "name": "Git config (nshafer)",
  "description": "Copies your git config and global ignore file from the host into the container when the container is created. Works in a container from the devcontainer CLI too, which copies nothing by itself.",
  "documentationURL": "https://github.com/nshafer/devcontainer-features/tree/main/src/git-config",
  "options": {},
  "postCreateCommand": "/usr/local/share/devcontainer/git-config/post-create.sh",
  "installsAfter": [
    "ghcr.io/devcontainers/features/common-utils"
  ]
}
